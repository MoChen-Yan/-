server:
  port: 8880

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/oldbusiness?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.jdbc.Driver
  thymeleaf:
    cache: false


mybatis:
  type-aliases-package: com.mochen.yiyuan.pojo
  mapper-locations: classpath:mybatis/mapper/*.xml